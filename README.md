# Leebook
- configure .env
- php artisan key:generate
- php artisan migrate
- php artisan serve
- (remember to make add the admin role, and add it to an user manually)
- composer install
- composer upgrade
- composer run dev
